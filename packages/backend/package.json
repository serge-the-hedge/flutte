{
	"name": "@blabla/backend",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "",
	"scripts": {
		"dev": "convex dev",
		"dev:setup": "convex dev --configure --until-success",
		"test": "vitest run",
		"test:locale-capacity": "BLABLA_LOCALE_CAPACITY_PROOF=1 vitest run convex/localeCapacity.integration.test.ts",
		"test:repository-proof": "vitest run --config vitest.repository.config.ts",
		"check-types": "tsc -p convex/tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
		"check-types:test": "tsc -p tsconfig.test.json --noEmit"
	},
	"dependencies": {
		"@convex-dev/aggregate": "^0.2.2",
		"@convex-dev/better-auth": "^0.12.5",
		"@convex-dev/migrations": "^0.3.5",
		"@convex-dev/rate-limiter": "^0.3.2",
		"@convex-dev/resend": "^0.2.7",
		"@convex-dev/workflow": "^0.4.4",
		"better-auth": "catalog:",
		"convex": "^1.45.0",
		"dotenv": "^17.4.2",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@blabla/config": "workspace:*",
		"@edge-runtime/vm": "^5.0.0",
		"@types/bun": "^1.3.14",
		"@types/node": "^24.13.3",
		"convex-test": "^0.0.54",
		"typescript": "^6.0.3",
		"vite": "^8.1.5",
		"vitest": "^4.1.10"
	}
}
